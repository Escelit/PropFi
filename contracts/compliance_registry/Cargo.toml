[package]
name = "propfi-compliance-registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "21", features = ["testutils"] }
propfi-types = { path = "../types" }

